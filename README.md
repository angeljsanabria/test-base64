# test-base64
```bash
sudo apt update -y
sudo apt install -y gcc-riscv64-unknown-elf qemu-system-misc gdb-multiarch
Es parte de QEMU, un emulador de sistemas completo.
qemu-system-misc incluye varios targets, como emuladores para: RISC-V, ARM
Podés correr el firmware .elf o .bin que compilaste y ver cómo se comportaría en un micro real.
gdb-multiarch = Es el GDB (GNU Debugger) con soporte para múltiples arquitecturas

```

